CREATE TABLE Players (uid INTEGER PRIMARY KEY, name TEXT DEFAULT "Nobody", rock INTEGER DEFAULT 100, paper INTEGER DEFAULT 100, scissors INTEGER DEFAULT 100, health INTEGER DEFAULT 100, deaths INTEGER DEFAULT 0, wins INTEGER DEFAULT 0, last_key TEXT DEFAULT 'None');
